# Enforce LF for shell scripts so they always work on Windows Git Bash.
# Without this, git on Windows rewrites .sh files with CRLF on checkout,
# which breaks bash (CRLF turns into literal \r in every line, producing
# "$'\\r': command not found" and "set: pipefail: invalid option name" errors).
*.sh text eol=lf
